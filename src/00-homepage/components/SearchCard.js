import React from 'react'

function SearchCard({ searchAllData }) {
  const {
    img,
    title,
    induction,
    content,
    creat_at,
    shop_name,
    shop_address_detail,
    name,
    mb_photo,
    mb_name,
    mb_email,
  } = searchAllData
  return <div>SearchCard</div>
}

export default SearchCard
